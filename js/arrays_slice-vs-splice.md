# Knowing the difference between splice() and slice()

- splice() changes the array permanently (mnemonic device: p is for permanent)
- slice() does not

You can also add items to an array with splice().
